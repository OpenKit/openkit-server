<%= form_for(@developer_data) do |f| %>
  <% if @developer_data.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@developer_data.errors.count, "error") %> prohibited this developer_data from being saved:</h2>

      <ul>
      <% @developer_data.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
