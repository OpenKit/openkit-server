namespace :bundler do
  before :install, 'bundler:set_mysql_dirs'
  task :set_mysql_dirs do
    on roles(:app) do
      execute "bundle config build.mysql2 --with-mysql-lib=/usr/lib/mysql --with-mysql-include=/usr/include/mysql"
    end
  end
end
